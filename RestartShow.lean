/-
File A — the file that DISPLAYS the imported value.

`#eval restartValue` below reflects edits to `restartValue` in
`RestartDef.lean` only AFTER you click "Restart File" here. Without the
restart, this worker keeps showing the value it loaded when it opened.
-/

import RestartDef

#eval restartValue
#print restartValue
